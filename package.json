{
  "name": "threejs-sphere",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "gh-pages": "^6.0.0",
    "gsap": "^3.12.2",
    "three": "^0.155.0"
  }
}
